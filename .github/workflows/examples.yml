name: Run Examples

on:
  push:
    branches: 
      - "**"
  pull_request:
  workflow_dispatch:

jobs:
  run-examples:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          fetch-depth: 0
      
      - name: Install uv
        uses: astral-sh/setup-uv@d1a83c327fd1f6ef1d25fe6e94ddc7eb5d20d1b1

      - name: Setup Python
        run: uv python install

      - name: Install dependencies
        run: uv sync

      - name: Generate Proto Files
        run: uv run python generate_proto.py

      - name: Prepare Hiero Solo
        id: solo
        uses: hiero-ledger/hiero-solo-action@97b1a5dfff3a18ae92ffb4c27f5ada95906f66bb
        with:
          installMirrorNode: true
        
      - name: Run Examples
        env:
          OPERATOR_ID: ${{ steps.solo.outputs.accountId }}
          OPERATOR_KEY: ${{ steps.solo.outputs.privateKey }}
          CHAIN_ID: 012A
          NETWORK: solo
        run: |
          files=$(ls examples/*.py)
          for file in $files;
          do
            echo -e "\n************ ${file} ************"
            uv run -m examples.$(basename "$file" .py)
          done