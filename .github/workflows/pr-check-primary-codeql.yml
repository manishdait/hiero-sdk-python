name: "CodeQL Advanced"

on:
    push:
        branches: ["main"]
    pull_request:
        branches: ["main"]
        paths-ignore:
            - "**/*.md"
    schedule:
        - cron: "28 23 * * *" # Runs every day at 23:28 UTC.

permissions:
  contents: read

jobs:
    analyze:
        name: Analyze (${{ matrix.language }})
        runs-on: hl-sdk-py-lin-md
        permissions:
            contents: read
            # required for all workflows
            security-events: write
            # required to fetch internal or private CodeQL packs
            packages: read

        strategy:
            fail-fast: false
            matrix:
                include:
                    - language: actions
                      build-mode: none
                    - language: python
                      build-mode: none
        steps:
            - name: Checkout repository
              uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v6.0.1

            - name: Harden the runner (Audit all outbound calls)
              uses: step-security/harden-runner@9af89fc71515a100421586dfdb3dc9c984fbf411 # v2.19.4
              with:
                  egress-policy: audit

            - name: Initialize CodeQL
              uses: github/codeql-action/init@7211b7c8077ea37d8641b6271f6a365a22a5fbfa # v4.3.5
              with:
                  languages: ${{ matrix.language }}
                  build-mode: ${{ matrix.build-mode }}
                  queries: security-extended
                  dependency-caching: true

            - name: Set up Python
              if: matrix.language == 'python'
              uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6.2.0
              with:
                  python-version: "3.13"

            - name: Install uv
              if: matrix.language == 'python'
              uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b # v8.1.0
              with:
                  enable-cache: true

            - name: Install dependencies
              if: matrix.language == 'python'
              run: uv sync --frozen --all-groups --all-packages --all-extras

            - name: Perform CodeQL Analysis
              uses: github/codeql-action/analyze@7211b7c8077ea37d8641b6271f6a365a22a5fbfa # v4.3.5
              with:
                  category: "/language:${{matrix.language}}"
